{
   "_id": "_design/poi",
   "language": "javascript",
   "views": {
       "byId": {
           "map": "function(doc) {\n  if (doc.type === 'journal') {\n    emit(doc._id, null)\n  }\n}"
       },
       "currentVersionById": {
           "map": "function(doc) {\n  if (doc.type === 'journal') {\n    var lastId = doc.refs.slice(-1)[0]\n    emit(doc._id, {'_id': lastId})\n  }\n}"
       }
   }
}