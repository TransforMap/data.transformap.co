version: '2'

services:
  web:
    build: .
    expose:
      - "5000"
    links:
      - geocouch:geocouch
    environment:
      NODE_ENV: 'production'
  geocouch:
    image: 14mmm/geocouch
    expose:
      - "5984"
